// Create a reference to your entity if you don't already have one
Entity entity = Entity.newInstance("http://www.someurl.com", "my_entity_name");

// Launch the share dialog with a listener
Socialize.getSocializeUI().share(this, entity, ShareType.FACEBOOK, new OnShareListener() {

	@Override
	public void onBeforeShare(Share share, ShareInfo info) {

		// Add any additional data for facebook here.
		// Refer to Facebook developer docs for complete list of options:
		// http://developers.facebook.com/docs/reference/api/post/
		
		info.getExtraParams().put("picture", "http://www.someurl.com/mypic.jpg");
	}

	@Override
	public void onError(Share share, SocializeException error) {
		// Handle error (Optional)
	}
	
	@Override
	public void onAfterShare(Share share, ShareType type) {
		// Handle success (Optional)
	}
}));

